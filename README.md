# Amazen

Minimal project scaffold for a labyrinth step-by-step web game.

## Structure

- `backend/` ASP.NET Core API skeleton
- `frontend/` React + Vite app with a placeholder game page

## Notes

- The backend currently exposes a sample endpoint at `/api/test-data`.
- The frontend currently renders a placeholder home page and displays the sample API response.
